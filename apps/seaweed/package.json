{
  "name": "@repo/seaweed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run docker:up:dev",
    "test": "npm run docker:up:test",
    "docker:up:dev": "docker-compose -f ./docker/dev/docker-compose.yaml up -d",
    "docker:up:test": "docker-compose -f ./docker/tests/docker-compose.yaml up -d"
  },
  "dependencies": {
    "dotenv-extended": "^2.9.0"
  }
}
